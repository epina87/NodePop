<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
            <div class="container px-4 px-lg-5">
                <div class="logo"></div>
            
                <div class="collapse navbar-collapse" id="navbarResponsive">
                    <ul class="navbar-nav ms-auto">
                        <% getLocales().forEach(locale =>{%>
                            <li class="nav-item">
                                <a class="nav-link" href="/change-locale/<%= locale %>"><%= locale %></a></li>
                        <% })%>            
                    </ul>
                </div>
            </div>
        </nav>

      <h1 class="welcom"><%= title %></h1>

      <p>
        <%= __(' Welcome to our announcements page') %>, 
        <%= __(' we are excited to have you') %>
        <%= __(' here and want to give you a warm welcome to our site.') %>

        <%= __(' We are your perfect destination to discover') %>
        
        <%= __(' a wide variety of advertisements, from products') %>
        
        <%= __(' and services to job opportunities and exciting events.') %>
              
      </p>
    </header>
  </body>
</html>
